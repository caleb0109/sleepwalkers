using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Audio;

/// <summary>
/// This script is used to randomly get a sound effect from the real world and inject it into the dream world
/// </summary>
public class RealitySoundManager : MonoBehaviour
{
    // public arrays to store the audio files
    public AudioClip[] intro;
    public AudioClip[] actOne;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
